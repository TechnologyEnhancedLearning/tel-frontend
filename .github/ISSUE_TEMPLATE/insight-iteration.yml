name: "Insight & Iteration"
description: "Record what we learned from research/testing and what we changed as a result."
title: "[Insight] or [Iteration] Short, descriptive title"
labels: ["UX", "Insight", "Iteration", "Learning"]
body:
  - type: input
    id: linked_design
    attributes:
      label: "🔗 Linked Design / Hypothesis issue"
      description: "Reference the related issue number (e.g., #234)."
      placeholder: "#234"
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: "🧭 Context"
      description: "Study/session details, audience, scenario, environment, devices."
      placeholder: "Round 7 moderated testing; 8 participants; mobile-first; task: confirm NHS number."
    validations:
      required: true
  - type: textarea
    id: findings
    attributes:
      label: "🗣 Key findings"
      description: "Distilled insights and supporting quotes/data."
      placeholder: "3/8 participants misread the confirmation hint; 2/8 failed to locate the Continue button."
    validations:
      required: true
  - type: textarea
    id: iteration
    attributes:
      label: "🔁 Changes made (if any)"
      description: "What we changed based on the findings."
      placeholder: "Moved primary action to bottom; strengthened hint contrast; simplified copy."
    validations:
      required: false
  - type: checkboxes
    id: outcome
    attributes:
      label: "📈 Outcome"
      options:
        - label: "Improved (meets success criteria)"
        - label: "Partially improved (follow-up needed)"
        - label: "No improvement"
        - label: "Regression (investigate)"
  - type: textarea
    id: metrics
    attributes:
      label: "📊 Evidence of impact"
      description: "Before/after metrics, qualitative signals, accessibility verification."
      placeholder: "Completion +12%, time on step −18%, focus visible on all controls; AT checks passed."
    validations:
      required: false
  - type: textarea
    id: next
    attributes:
      label: "📅 Next step"
      description: "What we’ll do next (e.g., retest, ship, new hypothesis, close)."
      placeholder: "Ready for build; schedule post-release analytics review in 2 weeks."
